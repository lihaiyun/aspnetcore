@{
    string? text = Convert.ToString(TempData["FlashMessage.Text"]);
    string? cssClass = "alert alert-" + Convert.ToString(TempData["FlashMessage.Type"]);
}
@if (!string.IsNullOrWhiteSpace(text))
{
    <div class="@cssClass" role="alert">
        @text
    </div>
}